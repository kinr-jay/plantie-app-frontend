   const GardenButton = (props) => {
    
    return (
        <button className="gardenButton"onClick={() => props.handleVisable()}>
            +
        </button>
    )
   }

   export default GardenButton